import React from 'react';
import { ICommand } from './Terminal';

const CommandResult = ({ command }: { command: ICommand }) => {
	return <div className="px-11">CommandResult</div>;
};

export default CommandResult;
